.DS_Store
.gitignore
node_modules
public
prof
tsconfig.tsbuildinfo
.obsidian
.quartz-cache
private/
.replit
replit.nix
.quartz/
quartz/cli/tui/dist/

# CI checkout of the content repo (content/ itself must NOT be gitignored:
# quartz's globber respects .gitignore and would see an empty site)
content-src/
